/**
 * Mental Wellness Coach - Root App Entry Point
 * 
 * This file serves as the entry point for the React Native app when run from the root directory.
 * It imports and re-exports the actual mobile app from the mobile/ directory.
 */

// Import the actual mobile app
import App from './mobile/App';

// Re-export it as the default export
export default App; 